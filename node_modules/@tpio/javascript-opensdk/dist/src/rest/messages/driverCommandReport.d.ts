import { Command } from 'selenium-webdriver';
import { IPayLoadDriverCommandReport } from '../../interfaces/iPayLoadDriverCommandReport';
export default class DriverCommandReport {
    private command;
    private result;
    private passed;
    private screenshot?;
    constructor(command: Command, result: unknown | undefined, passed: boolean, screenshot?: string | undefined);
    ToJson(): IPayLoadDriverCommandReport;
}
