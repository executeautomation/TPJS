export default class SocketManager {
    private static currInstance?;
    private socket?;
    static instance(): SocketManager;
    openSocket(socketAddress: string, socketPort: number): void;
    private isConnected;
    closeSocket(): void;
}
