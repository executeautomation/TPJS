import { Client } from 'webdriver';
import { RectReturn } from '@wdio/protocols/build/types';
import MobileCommandsReporter from '../../../internal/helpers/mobileCommandsReporter';
export default class MobileElement {
    private driverClient;
    private commandsReporter;
    readonly using: string;
    readonly value: string;
    readonly id: string;
    constructor(driverClient: Client, commandsReporter: MobileCommandsReporter, using: string, value: string, id: string);
    isEnabled(): Promise<boolean>;
    takeScreenshot(scroll?: boolean): Promise<string>;
    isSelected(): Promise<boolean>;
    isDisplayed(): Promise<boolean>;
    getAttribute(attr: string): Promise<string>;
    getText(): Promise<string>;
    getTagName(): Promise<string>;
    sendKeys(value: string): Promise<void>;
    clear(): Promise<void>;
    getCSSValue(propertyName: string): Promise<string>;
    getRect(): Promise<RectReturn>;
    click(): Promise<void>;
}
