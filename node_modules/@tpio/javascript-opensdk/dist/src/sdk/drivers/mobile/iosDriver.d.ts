import { By, Capabilities } from 'selenium-webdriver';
import MobileElement from './base/mobileElement';
import MobileDriver from './base/mobileDriver';
export default class IOSDriver extends MobileDriver {
    static createSession(caps: Capabilities): Promise<IOSDriver>;
    shake(): Promise<void>;
    rotateDevice(x: number, y: number, radius: number, rotation: number, touchCount: number, duration: number, element?: string): Promise<void>;
    touchId(match: boolean): Promise<void>;
    toggleEnrollTouchId(enabled?: boolean): Promise<void>;
    findElement(locator: By): Promise<MobileElement>;
}
