import { Command } from 'selenium-webdriver';
import CustomCommandExecutor from './seleniumCommandExecutor';
export default class RedactHelper {
    private customCommandExecutor;
    constructor(customCommandExecutor: CustomCommandExecutor);
    redactCommand(command: Command): Promise<unknown>;
    private redactionRequired;
    private isAndroidPasswordElement;
    private isSecuredElement;
}
