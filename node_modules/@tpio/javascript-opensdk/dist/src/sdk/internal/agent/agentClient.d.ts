import { Capabilities } from 'selenium-webdriver';
import AgentSession from '../session/agentSession';
import SessionResponse from '../../../rest/messages/sessionResponse';
import StepReport from '../../../rest/messages/stepReport';
import CustomTestReport from '../../../rest/messages/customTestReport';
import DriverCommandReport from '../../../rest/messages/driverCommandReport';
declare class AgentClient {
    private token;
    capabilities: Capabilities;
    private reportSettings;
    private remoteAddress;
    agentVersion?: string;
    agentSession: AgentSession;
    private asyncReportingQueue?;
    constructor(caps: Capabilities);
    startSession(): Promise<SessionResponse>;
    quitSession(): Promise<void>;
    reportTest(testReport: CustomTestReport): void;
    reportStep(stepReport: StepReport): void;
    reportDriverCommand(driverCommandReport: DriverCommandReport): void;
}
export default AgentClient;
