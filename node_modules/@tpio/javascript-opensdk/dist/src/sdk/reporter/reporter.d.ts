import AgentClient from '../internal/agent/agentClient';
declare type GetScreenshot = () => Promise<string>;
export default class Reporter {
    private agentClient;
    private getScreenshot;
    disableReports: boolean;
    disableCommandReports: boolean;
    disableTestAutoReports: boolean;
    constructor(agentClient: AgentClient, getScreenshot: GetScreenshot);
    step(description: string, message: string, passed: boolean, screenshot?: boolean): Promise<void>;
    test(name?: string, passed?: boolean, message?: string): void;
}
export {};
